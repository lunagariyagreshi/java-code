// in command line argument we can read value by s.o.p. if we write args[0] then it read only 1 name till space.
// we want read 2nd name then again write s.o.p args[1]
// to give input compile and run after run keep 1 space and write input.

// take input using command line argument.

public class CommandLine{
    public static void main(String[] args) {
     System.out.println(args[0]);   
     System.out.println(args[1]); 
    }
}
